#include<bits/stdc++.h>
using namespace std;

int main()
{
    int bg = 0;
    int ed = 0;
    int minNum, maxNum;
    int maxdis;
    int bg;
    vector<int> vec;
    while (1)
    {
        int n;
        scanf("%d", &n);
        vec.push_back(n);
        char c = getchar();
        if( c == '\n')
            break;
    }

    int len = vec.size();
    minNum = vec[0];
    maxNum = vec[0];
    maxdis = 1; 
    bg = 0;
    for(int i = 1; i < len; i++)
    {
        
    }
    
    return 0;
}