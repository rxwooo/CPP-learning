#include <bits/stdc++.h>
using namespace std;

int maxScoreSightseeingPair(vector<int> &values)
{
    int minPos = values[0] > values[1];
    int maxPos = values[0] < values[1];
    int len = values.size();
    for (int i = 0; i < len; i++)
    {
    }
}

int main()
{
    
    return 0;
}